
t = (2,4)
# print(t[2]) #  indexing starts from 0, so t[2] is the third item in tuple, which doesn't exist

# t.append(6)    # tuple is immutable! we can't add anything to it

a, b = t; print(a,b)    # an assignment, similar to a = 2; b = 4